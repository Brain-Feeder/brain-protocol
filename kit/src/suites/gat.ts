// GAT — Class D subset. Stub registrations; implemented in a later wave.
import { defineTest } from '../harness.js';
defineTest({ id: 'T-GAT-01', suite: 'GAT', cls: 'D', needs: 'none', name: 'no confirm no execution', clause: 'BP-08',
  run(ctx) { ctx.skip('GAT not yet implemented (pending wave)'); } });
defineTest({ id: 'T-GAT-02', suite: 'GAT', cls: 'D', needs: 'none', name: 'hash binds the yes', clause: 'BP-08',
  run(ctx) { ctx.skip('GAT not yet implemented (pending wave)'); } });
defineTest({ id: 'T-GAT-03', suite: 'GAT', cls: 'D', needs: 'none', name: 'idempotent execution', clause: 'BP-08',
  run(ctx) { ctx.skip('GAT not yet implemented (pending wave)'); } });
defineTest({ id: 'T-GAT-04', suite: 'GAT', cls: 'D', needs: 'none', name: 'expired drafts dead', clause: 'BP-08',
  run(ctx) { ctx.skip('GAT not yet implemented (pending wave)'); } });
defineTest({ id: 'T-GAT-09', suite: 'GAT', cls: 'D', needs: 'none', name: 'dark by default', clause: 'BP-08',
  run(ctx) { ctx.skip('GAT not yet implemented (pending wave)'); } });
