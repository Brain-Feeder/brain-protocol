// COM — Class D subset. Stub registrations; implemented in a later wave.
import { defineTest } from '../harness.js';
defineTest({ id: 'T-COM-01', suite: 'COM', cls: 'D', needs: 'none', name: 'staged-resync crash test', clause: 'BP-04',
  run(ctx) { ctx.skip('COM not yet implemented (pending wave)'); } });
defineTest({ id: 'T-COM-02', suite: 'COM', cls: 'D', needs: 'none', name: 'loop-guard echo test', clause: 'BP-04',
  run(ctx) { ctx.skip('COM not yet implemented (pending wave)'); } });
defineTest({ id: 'T-COM-03', suite: 'COM', cls: 'D', needs: 'none', name: 'hop cap', clause: 'BP-04',
  run(ctx) { ctx.skip('COM not yet implemented (pending wave)'); } });
defineTest({ id: 'T-COM-06', suite: 'COM', cls: 'D', needs: 'none', name: 'no foreign writes', clause: 'BP-04',
  run(ctx) { ctx.skip('COM not yet implemented (pending wave)'); } });
defineTest({ id: 'T-COM-07', suite: 'COM', cls: 'D', needs: 'none', name: 'malformed batch handling', clause: 'BP-04',
  run(ctx) { ctx.skip('COM not yet implemented (pending wave)'); } });
defineTest({ id: 'T-COM-08', suite: 'COM', cls: 'D', needs: 'none', name: 'live-query narrowness', clause: 'BP-04',
  run(ctx) { ctx.skip('COM not yet implemented (pending wave)'); } });
