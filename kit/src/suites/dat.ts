// DAT — Class D subset. Stub registrations; implemented in a later wave.
import { defineTest } from '../harness.js';
defineTest({ id: 'T-DAT-01', suite: 'DAT', cls: 'D', needs: 'none', name: 'adversarial visibility', clause: 'BP-02',
  run(ctx) { ctx.skip('DAT not yet implemented (pending wave)'); } });
defineTest({ id: 'T-DAT-02', suite: 'DAT', cls: 'D', needs: 'none', name: 'childrens wall (local)', clause: 'BP-02',
  run(ctx) { ctx.skip('DAT not yet implemented (pending wave)'); } });
defineTest({ id: 'T-DAT-03', suite: 'DAT', cls: 'D', needs: 'none', name: 'vault invisibility', clause: 'BP-02',
  run(ctx) { ctx.skip('DAT not yet implemented (pending wave)'); } });
defineTest({ id: 'T-DAT-04', suite: 'DAT', cls: 'D', needs: 'none', name: 'forget-to-zero', clause: 'BP-02',
  run(ctx) { ctx.skip('DAT not yet implemented (pending wave)'); } });
defineTest({ id: 'T-DAT-05', suite: 'DAT', cls: 'D', needs: 'none', name: 'forget receipt', clause: 'BP-02',
  run(ctx) { ctx.skip('DAT not yet implemented (pending wave)'); } });
defineTest({ id: 'T-DAT-06', suite: 'DAT', cls: 'D', needs: 'none', name: 'restore replay', clause: 'BP-02',
  run(ctx) { ctx.skip('DAT not yet implemented (pending wave)'); } });
defineTest({ id: 'T-DAT-07', suite: 'DAT', cls: 'D', needs: 'none', name: 'bounds hold', clause: 'BP-02',
  run(ctx) { ctx.skip('DAT not yet implemented (pending wave)'); } });
defineTest({ id: 'T-DAT-08', suite: 'DAT', cls: 'D', needs: 'none', name: 'rate limits', clause: 'BP-02',
  run(ctx) { ctx.skip('DAT not yet implemented (pending wave)'); } });
defineTest({ id: 'T-DAT-09', suite: 'DAT', cls: 'D', needs: 'none', name: 'journal immutability', clause: 'BP-02',
  run(ctx) { ctx.skip('DAT not yet implemented (pending wave)'); } });
defineTest({ id: 'T-DAT-10', suite: 'DAT', cls: 'D', needs: 'none', name: 'provenance totality', clause: 'BP-02',
  run(ctx) { ctx.skip('DAT not yet implemented (pending wave)'); } });
defineTest({ id: 'T-DAT-11', suite: 'DAT', cls: 'D', needs: 'none', name: 'local audit log', clause: 'BP-02',
  run(ctx) { ctx.skip('DAT not yet implemented (pending wave)'); } });
