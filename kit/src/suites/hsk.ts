// HSK — Class D subset. Stub registrations; implemented in a later wave.
import { defineTest } from '../harness.js';
defineTest({ id: 'T-HSK-01', suite: 'HSK', cls: 'D', needs: 'none', name: 'unsigned card refused', clause: 'BP-03',
  run(ctx) { ctx.skip('HSK not yet implemented (pending wave)'); } });
defineTest({ id: 'T-HSK-02', suite: 'HSK', cls: 'D', needs: 'none', name: 'matrix denial', clause: 'BP-03',
  run(ctx) { ctx.skip('HSK not yet implemented (pending wave)'); } });
defineTest({ id: 'T-HSK-03', suite: 'HSK', cls: 'D', needs: 'none', name: 'stolen token useless', clause: 'BP-03',
  run(ctx) { ctx.skip('HSK not yet implemented (pending wave)'); } });
defineTest({ id: 'T-HSK-04', suite: 'HSK', cls: 'D', needs: 'none', name: 'negotiation downgrade', clause: 'BP-03',
  run(ctx) { ctx.skip('HSK not yet implemented (pending wave)'); } });
defineTest({ id: 'T-HSK-05', suite: 'HSK', cls: 'D', needs: 'none', name: 'revocation immediate', clause: 'BP-03',
  run(ctx) { ctx.skip('HSK not yet implemented (pending wave)'); } });
defineTest({ id: 'T-HSK-06', suite: 'HSK', cls: 'D', needs: 'none', name: 'token lifecycle', clause: 'BP-03',
  run(ctx) { ctx.skip('HSK not yet implemented (pending wave)'); } });
